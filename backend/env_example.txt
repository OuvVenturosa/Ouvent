# CONFIGURAÇÕES PARA O VERCEL
# Copie e cole estas variáveis no formulário de Environment Variables do Vercel

NODE_ENV=production
DB_PATH=./ouvidoria.db
PORT=3001
CORS_ORIGIN=*
LOG_LEVEL=info
REQUEST_TIMEOUT=30000
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Configurações de Email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ouvidoria@venturosa.pe.gov.br
SMTP_PASS=sua_senha_email 